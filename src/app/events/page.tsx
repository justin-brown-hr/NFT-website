import { ComingSoon, comingSoonMetadata } from "@/components/ComingSoon";

export const metadata = comingSoonMetadata("Events");

export default function EventsPage() {
  return <ComingSoon title="Events" />;
}
